---
scan-name: "${scan_name}"
scanner-group-name: ${scanner_group_name}
target-group-name: ${target_group_name}
network-name: ${network_name}
banner-text: |
  ${indent(2, banner_text)}
ntp-server: ${ntp_server}
syslog-server: ${syslog_server}
group-name: "${group_name}"
user-name: "${user_name}"
credentials-name: ${credentials_name}
credentials-username: ""
credentials-private-key-file: ""
